#include <stdio.h>
#include "buttons.h"
#include "switches.h"
#include "supportFiles/display.h"
/*int main()
{
	display_init();  // Must init all of the software and underlying hardware for LCD.
	display_fillScreen(DISPLAY_BLACK);  // Blank the screen.
	display_setCursor(0, 0);            // The upper left of the LCD screen.
	buttons_runTest(); // run the buttons program first until terminate sequence is reached
	switches_runTest(); // run switches program last
    return 0;

}

void isr_function() {
       // Empty for now.
   }*/
